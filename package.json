{
  "name": "hugo-mod-jslibs",
  "version": "1.0.0",
  "description": "Hugo JS lib wrapper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm-run-all --parallel build:*",
    "build:@preact/bundle": "microbundle build --raw -f modern --cwd preact-bundle"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@preact/signals": "^1.2.2",
    "htm": "^3.1.1",
    "microbundle": "^0.15.1",
    "minisearch": "^6.3.0",
    "npm-run-all": "^4.1.5",
    "preact": "^10.19.6"
  }
}
