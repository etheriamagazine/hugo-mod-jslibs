{
  "name": "hugo-mod-jslibs",
  "version": "1.0.0",
  "description": "Hugo JS lib wrapper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm-run-all --parallel build:*",
    "build:preact-bundle": "microbundle build --raw --no-generateTypes -f cjs,esm,umd --cwd preact-bundle"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "microbundle": "^0.15.1",
    "@preact/signals": "^1.2.2",
    "htm": "^3.1.1",
    "preact": "^10.19.6"     
  }
}
